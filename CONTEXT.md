# Notify Me

Notify Me 是由 Codex 主 Agent 驱动的手机通知上下文。它只负责保存通知意图、判断结果的投递记录与 Bark 效果，不负责管理 Codex 任务标题、置顶或生命周期。

## Language

**通知条件**：
一条描述“何时应提醒用户”的语义规则。通知条件分为内置条件和任务作用域内的用户订阅。
_Avoid_: 触发等级、告警级别

**内置条件**：
Notify Me 固定提供的条件类型，包括任务阻塞和严重风险。每种内置条件可启用或关闭，并关联通知优先级和可选效果覆盖。
_Avoid_: 默认等级、系统告警

**用户订阅**：
用户在当前任务中主动创建的通知条件实例，可以一次性或重复生效。它关联一个通知优先级，并可选覆盖通知效果；未覆盖时继承优先级默认效果。
_Avoid_: 自定义告警、提醒口令

**订阅功能开关**：
控制是否允许创建和恢复用户订阅的全局能力开关。它不是一条通知条件，关闭时不改变内置条件。
_Avoid_: Subscription 条件、默认订阅

**任务阻塞**：
原定目标因等待用户授权、信息、选择或操作等原因无法由主 Agent 继续推进的状态。
_Avoid_: 普通失败、暂时困难、可自动恢复的问题

**严重风险**：
任务继续执行可能造成灾难性或大范围安全、数据、成本、法律或不可逆后果，或原有保护与回滚保证已经失效，需要用户立即介入的状态。
_Avoid_: 一般错误、普通警告、已获授权的常规敏感操作

**通知优先级**：
通知条件的语义紧迫度，取值为 P0、P1、P2 或 P3。优先级提供默认通知效果，但不直接等同于 Bark 的 `level`。
_Avoid_: 通知效果、Bark level

**通知效果**：
一组可复用的 Bark 展示与打扰参数。条件可以覆盖其优先级的默认效果。
_Avoid_: 通知优先级、风险等级

**有效通知效果**：
一次通知最终使用的效果；优先采用条件自己的效果覆盖，否则采用该条件优先级的默认效果。
_Avoid_: 条件等级、最终优先级

**打扰强度**：
通知效果中可比较的打扰程度，用于判断同一事项是否发生严格升级。铃声名称、归档策略和投递有效期不定义打扰强弱。
_Avoid_: 效果指纹、通知优先级

**通知事项**：
一段时间内持续存在的同一个阻塞、风险或订阅目标，用于关联发生、升级、解决和再次发生。
_Avoid_: 消息、请求

**通知事件**：
通知事项中一次值得提醒用户的状态变化。相同状态的重复报告不是新事件。
_Avoid_: 重试、轮次

**通知升级**：
同一通知事项的打扰强度严格增加，或发生新的语义状态，因此获得再次提醒资格。不可比较的效果变化本身不构成升级。
_Avoid_: 插件升级、重复发送

**任务作用域**：
由一个 Codex 顶层任务派生的隔离边界。订阅、事项、事件和去重记录不会跨任务互相影响。
_Avoid_: 项目、工作目录

**作用域解析**：
Hook 与 CLI 通过同一解析器把宿主提供的顶层任务身份转换为任务作用域。身份缺失、冲突或无法证明一致时必须停止通知和订阅恢复。
_Avoid_: 当前目录推断、调用者传入 scope

**主通知者**：
当前顶层任务中唯一可以触发 Notify Me 的主 Agent。子 Agent 与 Ticket Worker 只向主 Agent 报告。
_Avoid_: 任意 Agent、发送进程

**规则绑定**：
用户授权写入当前 `CODEX_HOME` 中实际生效的全局 AGENTS 文件的 Notify Me 托管规则，使主 Agent 在约定条件下调用技能。若 `AGENTS.override.md` 遮蔽普通文件，必须展示并处理该事实；规则在新顶层任务中验证后才视为生效。
_Avoid_: Hook、自动监听

**订阅上下文恢复**：
由 `UserPromptSubmit` 与 `SessionStart(source=compact)` 协同执行的本地、只读优先、幂等恢复过程。没有有效订阅时不向模型增加上下文；两种事件共享任务作用域与 `context_revision`。
_Avoid_: 每轮 `$notify-me check`、Stop Hook、后台监控

**激活**：
从只读检查、私密 Bark 绑定、效果确认、规则授权、双 Hook 信任到新任务验活的可恢复流程。SQLite 由插件自动创建，不要求数据库服务、Docker 或手工 SQL。
_Avoid_: 单次 setup 命令、静默修改全局配置

**服务已接受**：
Bark 服务已接受通知请求，但手机是否展示或播放声音仍未验证。
_Avoid_: 已收到、已送达手机
